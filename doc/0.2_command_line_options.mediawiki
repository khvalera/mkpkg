== Опции командной строки ==
Данный раздел описывает опции используемые для управления mkpkg из командной строки. 

=== Опции используемые в mkpkg: === 

{| style="color: black; background-color:#ffffcc;" width="85%" class="wikitable"
! style="width: 40%; background-color: #ffffcc;"| '''Опция''' 
! style="width: 100%; background-color: #ffffcc;"| '''Описание'''
|-
| <> || При выполнении скрипта без опций, mkpkg попытается найти и использовать ABUILD из текущей директории (из которой был запущен скрипт mkpkg).
|-
|'''-bt --build-tree'''             || Позволяет использовать дерево сборки пакетов. Параметром опции есть локальный файл (имя.версия.build_tree.tar.xz), также можно указать ссылку на файл, используя протокол http,ftp или воспользоваться параметром api:имя пакета (файл находится в собранном пакете в usr/src/BuildTrees).
|-
|'''-ubt --unpack_build_tree=PKG''' || Извлечь из пакета PKG файл дерева сборки.
|-
|'''-bv --bump-version=NEW_VER'''   || Изменяет текущую версию пакета в ABUILD на NEW_VER (в ABUILD переменная pkgver).
|-
|'''-bb --bump-build=NEW_BUILD'''   || Изменить текущий билд в ABUILD на NEW_BUILD (в ABUILD переменная pkgbuild).
|-
|'''-ib'''                          || Автоматически увеличить номер билда на единицу (в ABUILD переменная pkgbuild).
|-
|'''-i --install'''                 || Установить пакет после сборки (только от ROOT). Если нельзя собирать из под root …?
|-
|'''-si --sudo-install'''           || Установить пакет после сборки (необходимо прежде настроить sudo).
|-
|'''-h --help'''                    || Вывести эту справку.
|-
|'''-nc --no-cleanup'''             || Не очищать временные директории после сборки (по умолчанию /tmp/mkpkg_tmp/имя пакета). Бывает полезно при ошибках, можно проанализировать лог сборки пакета. 
|-
|'''-ns'''                          || Не выполнять strip для пакета.
|-
| '''r'''                           || Перед сборкой не распаковывать исходники и не очищать временные директории (по умолчанию /tmp/mkpkg_tmp/имя пакета). Это бывает полезно для возобновления прервавшейся из-за ошибки сборки.
|-
|'''-c --copy-abuild'''             || Скопировать пустой шаблон в текущую директорию. Дополнительным параметром можно указать имя шаблона системы сборки. Список имён:
 cmake qmake python perl waf scons. 
По-умолчанию используется autotools. Для пустого шаблона использутся 'c'. 
|-
|'''-wd'''                          || Имя директории, где будет проходить сборка пакета (по умолчанию /tmp/mkpkg_tmp).
|-
|'''-pod'''                         || Имя директории, куда будет помещён готовый пакет (по умолчанию ~/.mkpkg/packages).
|-
|'''-wdm'''                         || Режим выбора временных каталогов:
 cwd    - все операции проходят в текущем каталоге.
 remote - используются пути из конфигурационного файла.
 auto   - пути из конфигурационного файла используются 
          только  при сборке из дерева (build tree).
|- 
|'''-ccd'''                         || Очистить директорию с исходниками (по умолчанию ~/.mkpkg/source_cache).
|-
|'''-cpd'''                         || Очистить директорию с готовыми пакетами (по умолчанию ~/.mkpkg/packages).
|-
|'''-po --parent-only'''            || Собирает только основной пакет в мультипакетных ABUILD (пропускаются пакеты перечисленные в переменной pkglist).
|-
|'''-ad --append-deps'''            || Добавлять после сборки зависимости пакета в ABUILD (переменная build_deps).
|-
|'''-64 пакет/директория'''         || Конвертировать 32битный пакет в 64 битный без замены директорий (для подобных пакетов wine и skype).
|-
|'''-c64 пакет/директория'''        || Конвертировать 32битный пакет в 64битный пакет с заменой директорий (compat32 пакеты).
|-
|'''-sv --skip-validate'''          || Выключить валидацию пакета после сборки.
|-
|'''-sgd --skip-gendeps'''          || Не генерировать зависимости для пакета.
|-
|'''-v --version'''                 || Показать версию скрипта и выйти.
|-
|'''-d --debug'''                   || Включить отладку (set -xv). Так же включается и опция -nc
|}

=== Пример ===
Пересборка пакета mousepad c автоматическим увеличить номер билда 
<pre>
mkpkg -si -bt api:mousepad -ib
</pre>